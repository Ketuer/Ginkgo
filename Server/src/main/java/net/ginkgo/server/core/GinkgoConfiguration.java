package net.ginkgo.server.core;

import net.ginkgo.server.config.GlobalConfig;
import net.ginkgo.server.config.NetworkConfig;

public class GinkgoConfiguration {
    static NetworkConfig networkConfig = new NetworkConfig();
    static GlobalConfig globalConfig = new GlobalConfig();
}
